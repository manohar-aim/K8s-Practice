apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: roboshop
  region: us-east-2

managedNodeGroups:
  - name: roboshop-nodes
    instanceType: t3.micro
    desiredCapacity: 1
    minSize: 1
    maxSize: 1
    